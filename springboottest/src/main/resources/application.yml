server:
  port: 8888
  servlet:
    context-path: /test
spring:
  application:
    name: baseproject
  datasource:
    url: jdbc:mysql://localhost:3306/smbms?serverTimezone=Hongkong&zeroDateTimeBehavior=convertToNull
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
management:
  endpoint:
    health:
      show-details: always
mybatis:
  mapper-locations: classpath:/mapper/*Mapper.xml
  type-aliases-package: com.example.springboottest.pojo